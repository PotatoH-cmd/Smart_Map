# 报告生成工具集成知识库检索 - 改进说明

## 📋 问题描述

原有的报告生成工具（`report_generator_tool`）没有参考知识库检索的内容，导致生成的报告缺少相关政策、规范、流程等重要信息。

## ✅ 改进内容

### 1. 更新工具描述

**文件**: `backend/tools/report_generator_tool.py`

在工具描述中明确添加了知识库检索的要求：

```python
description = '''
报告生成工具。用于根据提供的变量数据，基于固定模板生成 Word 文档报告。
使用场景：当用户请求生成报告、文档或总结时使用。

重要：生成报告前，你应该先检索知识库获取相关政策、规范、流程等信息！

使用步骤：
1. 调用 knowledge_base_tool(operation='search', query='相关主题') 检索知识库
2. 调用 postgresql_tool 获取业务数据
3. 整理这些数据，生成用于填充模板的变量文本
4. 调用此工具，传入 variables 字典（应包含知识库检索结果）
'''
```

### 2. 更新系统提示词

**文件**: `backend/main.py`

在 Agent 的系统提示词中，明确规定了报告生成的完整流程：

```
4. 报告生成

**重要：生成报告前必须先检索知识库！**

报告生成流程：
① 调用 knowledge_base_tool(operation='search', query='报告主题相关的政策、规范、流程')
② 调用 postgresql_tool 获取业务数据
③ 整合知识库内容和业务数据，生成报告变量
④ 调用 report_generator_tool 生成报告

示例：
用户：生成固始县砂场超深度开采分析报告

正确流程：
1. knowledge_base_tool(operation='search', query='超深度开采判定规则 固始县')
2. postgresql_tool(operation='query', sql='SELECT ... FROM ceshen WHERE ...')
3. 整理知识库检索结果和数据库查询结果
4. report_generator_tool(variables={
     'report_title': '固始县砂场超深度开采分析报告',
     'summary': '根据《采砂管理条例》...（包含知识库内容）',
     'details': '...',
     'conclusion': '...',
     ...
   })
```

## 🔄 改进后的工作流程

### 之前的流程（缺少知识库）

```
用户请求生成报告
  ↓
查询数据库获取业务数据
  ↓
直接生成报告（缺少政策依据）
```

### 改进后的流程（包含知识库）

```
用户请求生成报告
  ↓
① 检索知识库获取相关政策、规范、流程
  ↓
② 查询数据库获取业务数据
  ↓
③ 整合知识库内容 + 业务数据
  ↓
④ 生成包含政策依据的完整报告
```

## 📊 知识库内容覆盖

当前 RagFlow 知识库（信阳市智慧巡河报告）包含：

- **第 1-3 章**: 项目概况、组织实施、项目汇总
- **第 5 章**: 各县区采砂场监测评估意见（平桥区、罗山县、潢川县、固始县、息县、商城县、新县、光山县）
- **第 6 章**: 总结与建议

总计 **84 个文档**，包含：
- 404 个图片
- 7 个表格
- 完整的政策规范、技术标准、操作流程

## 💡 使用示例

### 示例 1: 生成超深度开采分析报告

```
用户: 生成固始县砂场超深度开采分析报告

Agent 执行流程:

1. 检索知识库
   knowledge_base_tool(
     operation='search',
     query='超深度开采判定规则 固始县 采砂管理'
   )
   ↓ 获取到：
   - 超深度开采的定义和判定标准
   - 固始县采砂场监测评估意见
   - 相关政策规定

2. 查询数据库
   postgresql_tool(
     operation='query',
     sql='SELECT AVG("Control_Elevation" - "Measured_Depth") as avg_diff 
          FROM ceshen 
          WHERE "County_District" = \'固始县\''
   )
   ↓ 获取到：
   - 固始县各砂场的实测高程和控制高程数据
   - 超深度统计结果

3. 整合数据生成报告
   report_generator_tool(
     variables={
       'report_title': '固始县砂场超深度开采分析报告',
       'summary': '根据《河南省河道采砂管理规定》和《信阳市智慧巡河监管项目报告》，超深度开采定义为实测高程低于控制高程2米以上。经查询数据库，固始县共有X个砂场存在超深情况...',
       'details': '（包含知识库中的政策依据 + 数据库查询结果）',
       'conclusion': '...',
       'recommendations': '...'
     }
   )
```

### 示例 2: 生成采砂场监测评估报告

```
用户: 生成罗山县采砂场监测评估报告

Agent 执行流程:

1. 检索知识库
   knowledge_base_tool(
     operation='search',
     query='罗山县采砂场监测评估 无人机航测 水下测量'
   )
   ↓ 获取到：
   - 罗山县采砂场监测评估意见（12个采砂场）
   - 无人机航测技术规范
   - 水下测量技术要求

2. 查询数据库
   postgresql_tool(...)
   ↓ 获取到最新监测数据

3. 生成完整报告
   report_generator_tool(variables={...})
```

## 🎯 预期效果

### 报告质量提升

1. **政策依据充分**: 报告引用相关法规、标准、规范
2. **数据准确可靠**: 结合实时业务数据
3. **建议具有针对性**: 基于知识库中的专家建议和历史经验
4. **格式规范统一**: 使用标准模板，确保报告质量

### 报告内容示例

```
【报告标题】固始县砂场超深度开采分析报告

【摘要】
根据《河南省河道采砂管理规定》（知识库检索结果）和《信阳市智慧巡河
监管项目报告》第5章第6节（知识库检索结果），超深度开采的判定标准为：
整个砂场的实测高程与控制高程的平均差值超过2米。

经查询数据库（postgresql_tool查询结果），固始县共有X个砂场，其中
Y个砂场存在超深度开采情况，平均超深Z米...

【详细分析】
（包含知识库中的技术规范 + 数据库查询的详细数据）

【结论与建议】
（基于知识库第6章的建议 + 实际数据分析）
```

## ✅ 验证清单

- [x] 工具描述已更新
- [x] 系统提示词已更新
- [x] 后端服务已重启
- [x] 知识库已就绪（84个文档）
- [ ] 测试报告生成功能
- [ ] 验证知识库内容被正确引用

## 📝 注意事项

1. **Agent 自主性**: Agent 会根据提示词自主决定是否检索知识库，建议用户明确要求"生成包含政策依据的报告"

2. **知识库覆盖**: 当前知识库主要覆盖信阳市智慧巡河项目相关内容，如需其他主题需要补充知识库

3. **检索关键词**: 建议 Agent 使用精确的关键词检索，如"超深度开采 固始县"而非简单的"开采"

4. **变量整理**: Agent 需要将知识库检索结果和数据库查询结果整合后，再生成报告变量

## 🚀 下一步优化建议

1. **自动化知识库引用**: 在报告模板中增加 `{knowledge_references}` 占位符，自动插入知识库检索结果

2. **多轮检索**: 支持对不同主题进行多次知识库检索（政策、技术、案例等）

3. **引用标注**: 在报告中自动标注信息来源（如"根据《XXX规定》..."）

4. **知识库扩展**: 持续补充更多政策法规、技术标准、典型案例

---

**改进日期**: 2026-04-14  
**状态**: ✅ 已完成  
**相关文件**: 
- `backend/tools/report_generator_tool.py`
- `backend/main.py`
